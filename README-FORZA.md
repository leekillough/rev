# forzarev : FORZA SST Models

```
git clone git@github.com:tactcomplabs/forzarev.git
cd forzarev
git checkout devel
git clone https://github.gatech.edu/FORZA/sst-forza-elements.git forza-elts
cd forza-elts
git checkout <branch_name; devel is the stable branch>
```
